# ios only. add these to your Xcode project (e.g. Config.swift or .xcconfig)
# for the web app, use web/.env.example instead, the variable names differ

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
